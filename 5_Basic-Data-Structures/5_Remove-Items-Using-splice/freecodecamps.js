const arr = [2, 4, 5, 1, 7, 5, 2, 1];
// Only change code below this line

// remove the "1" element(s) starting at index "0"
arr.splice(0,1)

// remove the "4" element(s) starting at index "3"
arr.splice(3,4)

// Only change code above this line
console.log(arr);