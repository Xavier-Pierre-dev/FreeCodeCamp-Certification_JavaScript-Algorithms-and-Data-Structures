const arr1 = ['JAN', 'FEB', 'MAR', 'APR', 'MAY'];
let arr2;

// we use the spread operator for pass each element of the array arr1 to the second array
arr2 = [...arr1];  // Change this line

console.log(arr2);