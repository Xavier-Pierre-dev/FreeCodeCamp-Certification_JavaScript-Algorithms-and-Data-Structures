// Only change code below this line
// value have a default "1" so if we call the function without value as argument the value will be considered as equal to 1
const increment = (number, value = 1) => number + value;
// Only change code above this line